name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  pass:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: ✅ Project ready
        run: echo "ai-router — smart LLM routing, RAG, agents"
